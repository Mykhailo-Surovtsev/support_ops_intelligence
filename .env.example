# Leave both values empty to run the safe local rules fallback.
OPENAI_API_KEY=
OPENAI_MODEL=

# Optional CRM endpoint. It receives a JSON POST request after ticket persistence.
CRM_WEBHOOK_URL=
CRM_API_KEY=

# Leave empty for a local demo. Set a long random value before sharing the stack.
API_SHARED_SECRET=